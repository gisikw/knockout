# Built-in harness for Claude Code CLI
# Prompt is passed via stdin when using -p flag
binary: claude
args:
  - "-p"
  - "--output-format"
  - "text"
  - "${allow_all}"
  - "${allowed_tools}"
  - "${model}"
  - "${system_prompt}"
