Looking at this ticket:

**"Add snooze field to ticket frontmatter. ISO 8601 date (e.g. snooze: 2026-05-01). Settable via ko add and adjustable via ko update."**

This is clearly describing **what to build**: a new snooze field with specific requirements (ISO 8601 format, settable/adjustable via specific commands). It's a feature request, not reporting a problem or asking for investigation.

```json
{"disposition": "route", "workflow": "task"}
```
