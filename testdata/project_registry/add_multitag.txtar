# Multiple tags: first routes, rest are labels
env HOME=$WORK/home
mkdir $WORK/home

# Register target project
mkdir $WORK/target/.tickets
cd $WORK/target
exec ko register '#target'
cd $WORK

# Add with multiple tags
exec ko add 'Refactor auth #target #security #urgent'

# Verify ticket in target has extra tags as labels
env TICKETS_DIR=$WORK/target/.tickets
exec ko query
stdout 'Refactor auth'
stdout '"status":"routed"'
stdout 'security'
stdout 'urgent'

-- .tickets/ko-seed.md --
---
id: ko-seed
status: closed
deps: []
links: []
created: 2026-01-01T00:00:00Z
type: task
priority: 2
---
# Seed
