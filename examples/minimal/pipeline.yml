# Minimal pipeline -- just implement, no triage or review gates
model: claude-sonnet-4-5-20250929
max_retries: 1
discretion: high
# require_clean_tree: true  # Prevent builds when uncommitted changes exist (outside .ko/)

workflows:
  main:
    - name: implement
      type: action
      prompt: implement.md
