# Tickets directory created on demand
# Use a named directory so the derived prefix is predictable
mkdir myproject
cd myproject
exec ko create 'First ticket'
stdout '^myp-[0-9a-f]{4}$'
exists .ko/tickets
