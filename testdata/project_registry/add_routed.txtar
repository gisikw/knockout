# Add with recognized tag routes to target project
env HOME=$WORK/home
mkdir $WORK/home

# Register target project
mkdir $WORK/target/.ko/tickets
cd $WORK/target
exec ko project set '#target'
cd $WORK

# Add a routed ticket
exec ko add 'Fix the thing #target'
stdout '#target'

# Verify ticket exists in target project with status routed
env TICKETS_DIR=$WORK/target/.ko/tickets
exec ko query
stdout 'Fix the thing'
stdout '"status":"routed"'

# Verify audit ticket in local project is closed
env TICKETS_DIR=$WORK/.ko/tickets
exec ko query
stdout 'Fix the thing'
stdout '"status":"closed"'

-- .ko/tickets/ko-seed.md --
---
id: ko-seed
status: closed
deps: []
created: 2026-01-01T00:00:00Z
type: task
priority: 2
---
# Seed
