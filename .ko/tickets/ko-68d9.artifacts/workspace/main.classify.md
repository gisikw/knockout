knockout — just --list for recipes
```json
{"disposition": "route", "workflow": "task"}
```

The ticket describes a specific code change: consolidate two configuration files into one. It states what should be built (a single config.yaml), not what's broken, and has a clear expected outcome (committed configuration changes).
