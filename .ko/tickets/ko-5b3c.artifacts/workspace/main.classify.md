```json
{"disposition": "route", "workflow": "task"}
```

This ticket requests a **feature implementation**: adding a "hidden" flag for projects (similar to the existing `--default` flag), excluding hidden projects from `ko project ls` by default, and adding a `--all` flag to show them. The requirements are clear and specific.
