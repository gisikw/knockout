# When no default is set, project ls shows no asterisk
env HOME=$WORK/home
mkdir $WORK/home

# Register without setting default
exec ko project set '#exo'

# project ls shows no asterisk marker
exec ko project ls
stdout '  exo'
! stdout '\* exo'

-- .ko/tickets/.keep --
